# Contract Spine Prompt Pack v0.1

This is the execution companion for the active Contract Spine issue stack.

## Purpose
Keep issue files as the authority/spec layer.
Keep prompt files as the execution layer.

## Operating rule
- use Contract Spine naming only
- use the issue spec, naming lock, active outputs, and THEO review comments as the authority slice
- default to one build prompt and one review or patch prompt
- use an extra migration or verification prompt only when live surface sync is required
- keep active Floor scope narrow: active control docs only, not historical sweep

## Companion files
- leny/prompt_packs/contract_spine_prompt_index_v0.1.md
- leny/prompt_packs/issue01_signoff_patch_prompt_v0.1.md

## Prompt count target
Plan around 16 prompts total for the full Contract Spine stack.
